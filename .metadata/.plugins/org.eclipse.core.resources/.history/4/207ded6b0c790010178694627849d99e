package com.example.demo.service;

import java.util.List;

import com.example.demo.entity.surveyList;
import com.fasterxml.jackson.core.JsonProcessingException;

import jakarta.persistence.Id;

public interface surveyListService {
	surveyList createSurvey(surveyList surveyList);
	List<surveyList> getAllSurvey();
	surveyList getSurveyId(int id);
	List<surveyList> getSurveyName(String surveyName);
	void deleteSurvey(int id);
	surveyList updateSurvey(surveyList surveyList);
	String convert(Object Json) throws JsonProcessingException();
	
	
	
	

}
